import 'package:flutter/material.dart';

class FigmaSection extends StatelessWidget {
  const FigmaSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('figma section');
  }
}
